#include <windows.h> // Windows API for process manipulation, memory management, etc.
#include <TlHelp32.h> // For creating process snapshoots and retrieving process info
#include <iostream>// for console ouput and other basic stuff 
#include <string> // For using std::string to handle DLL paths and process names

// Function to inject the DLL into Minecraft
bool InjectDLL(DWORD processID, const std::string& dllPath) {
    // Step 1: Open the target process with all access rights
}

//step 2: go cry in the corner cuz c++ sucks balls 
// step 3: kill yourself
//step 4: happy now that you cant do c++ as you are dead
// step 5: sad because you can't program in other languages

//step 6


